expressions auflösen in:

Field assignments
global var decls.
function/method bodies
constructors
static blocks

was für felder sind definiert und müssen im code referenziert werden?
(falls vars umbenannt werden müssen diese referenzen auch noch den Namen kennen)

Globale vars
Felder
Locale vars
parameter